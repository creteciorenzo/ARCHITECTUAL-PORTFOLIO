import { createTheme } from '@nextui-org/react'
const theme = createTheme({
  type: 'dark',
  theme: {
    colors: {
      background: '#f7f4ed',
      text: '#040303',
      tertiary: '#aaa',
    },
  },
})

export default theme
